import { Components } from "@mui/material";

export const components: Components = {
  MuiButton: {
    styleOverrides: {
      root: {
        textTransform: "none",
        borderRadius: 8
      }
    }
  }
};
