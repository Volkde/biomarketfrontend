import { Product } from "types/Product";

/**
 * Product card component props interface
 * @interface ProductCardProps
 * @property {Product} product - Product data
 */
export interface ProductCardProps {
  product: Product;
}
